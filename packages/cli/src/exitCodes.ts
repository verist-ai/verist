// SPDX-License-Identifier: Apache-2.0

export const EXIT_OK = 0;
/** Diffs detected (used by `verist test`). */
export const EXIT_DIFF = 1;
/** Fatal error (bad config, invalid input, etc). */
export const EXIT_ERROR = 2;
